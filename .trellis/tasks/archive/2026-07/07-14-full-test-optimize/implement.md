# Implement — 执行清单

## 环境准备
- [ ] 确认 Browser Relay 健康（`curl /api/debug`），目标 tab 已登录 xiaocan。
- [ ] 确认后端日志访问：ssh root@121.91.175.192（已验证），`journalctl -u xiaocan`。

## 按模块测试（顺序执行）
### 1. 设置 `/settings`
- [ ] 加载页面，snapshot 看代理IP池/登录态/账号区。
- [ ] 读 network 看拉取配置接口响应。
- [ ] 测代理配置保存（用现有值重存，观察是否生效/报错）。
- [ ] 记录问题。

### 2. 地址管理 `/location`
- [ ] 加载，snapshot 地址列表。
- [ ] 新增一个测试地址 → 复核 → 删除测试地址（可恢复）。
- [ ] 测推送绑定（LocationPushTarget）查看态。
- [ ] 记录问题。

### 3. 首页活动列表 `/`
- [ ] 加载，记录"需多次刷新"现象次数/耗时。
- [ ] 测搜索门店、排序切换（默认/返现金额/返现比例/距离）、3km过滤。
- [ ] snapshot/network 看列表接口与排序逻辑（前端排序 vs 后端分页）。
- [ ] 记录问题。

### 4. 卡券查询 `/grab-card`
- [ ] 加载，触发查询，看饭票数量。
- [ ] network 看查询接口；验证饭票 cardId 按 name=="饭票" 判断（记忆 ticket-cardid-not-fixed）。
- [ ] 记录问题。

### 5. 监控管理 `/monitor`
- [ ] 加载，snapshot 监控配置列表。
- [ ] 新增测试监控 → 复核 → 删除（可恢复）。
- [ ] 记录问题。

### 6. 通知记录 `/notify-history`
- [ ] 加载，snapshot 历史列表，分页/过滤。
- [ ] 记录问题。

### 7. 抢单记录 `/grab-history`
- [ ] 加载，snapshot 历史。
- [ ] 记录问题。

### 8. 抢单 `/grab`（高风险，最后，已确认实际下单）
- [ ] 加载，snapshot 抢单/定时抢单配置。
- [ ] 测配置保存、cron/一次性定时配置校验。
- [ ] 测饭票校验拦截逻辑。
- [ ] ssh `journalctl -u xiaocan -f` 开后台日志，前端选可抢活动实际触发抢单。
- [ ] 同步观察：前端 network 小蚕接口响应、后端日志、WAF/风控/代理命中。
- [ ] 记录结果与风控响应。

## 汇总
- [ ] 整理 `report.md`：模块结果表 + 问题清单（分级、复现、建议）+ 不稳定现象分析 + 抢单风险说明。
- [ ] 自检：8 模块全覆盖、前后端双侧记录齐全、问题可复现。

## 回滚点
- 测试中创建的地址/监控等测试数据，测完删除恢复。
- 不修改任何后端代码/数据库（本任务为测试+报告，修复另行进 execute）。
